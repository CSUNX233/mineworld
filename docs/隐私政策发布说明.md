# 隐私政策发布

运营主体：桑尼工作室。隐私邮箱：1977975227@qq.com。政策版本：2026-09-14。

正文唯一来源为 `public/privacy.html`，Vite 复制到网站根目录，Capacitor 同步时复制进 APK。主菜单与首次隐私提示使用同一文件，不依赖远程网页读取。网站链接为 `https://mineworld-c5z.pages.dev/privacy`，Vercel 镜像为 `https://mineworld-rose.vercel.app/privacy.html`；填写前确认已部署且返回政策正文。

首次同意前不加载 Game 模块或创建 Game，拒绝保留提示。只保存政策版本，撤回只删除 `mineworld-privacy-consent` 并重新载入；不修改五个存档、研究、设置或旧数据备份。无存储权限时仅本次会话有效。主菜单有常驻入口；政策弹窗有固定关闭按钮。

本次是政策网页及前端接入，不是隐私检测报告或实名认证、防沉迷 SDK 接入。当前无账号、广告、付费及身份核验服务；未来接入时必须据实更新正文及政策版本，完成新增授权和平台审核。工作室名称须与实际后台运营主体一致。

新版 APK 1.0.2（versionCode 3）已包含隐私界面，旧版 1.0.1 不包含。发布 Android 更新时需用原签名、递增 versionCode 重新构建；不能只更新网站便认为本地资源 APK 同步更新。平台提审应使用带政策入口的新包，并完成所要求的检测和防沉迷材料。

基础验证：TypeScript / Vite 构建；移动端 1000×460 全文显示及关闭、390×844 首次选择；拒绝不创建游戏、同意进入主菜单、撤回后回到提示。未进行真实 Android 隐私检测。

## TapTap 后台填写链接（2026-09-14 核验）

推荐填写 `https://mineworld-c5z.pages.dev/privacy`。该地址经本机直连及代理访问均返回 HTTP 200，正文与 `public/privacy.html` 完全一致。Cloudflare 的 `/privacy.html` 地址会发生 308 重定向，因此后台使用不带扩展名的直接地址。此核验不代表 TapTap 审核服务器必然可访问，保存后仍需在后台重新检测。

游戏内入口继续使用随包附带的本地政策文件，无须为了更换后台链接重新安装 APK。
